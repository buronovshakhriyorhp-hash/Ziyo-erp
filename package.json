{
    "name": "ziyo-erp-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd .. && mkdir public && xcopy /E /Y \"frontend\\dist\\*\" \"public\\\"",
        "start": "node backend/dist/server.js"
    }
}